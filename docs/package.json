{
  "name": "docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "generate": "bun scripts/update-page-dates.ts && bun scripts/update-package-meta.ts && bun scripts/build-snippets.ts",
    "build": "bun run generate && tsc --noEmit && vite build && bun scripts/build-sitemap.ts",
    "preview": "bun run build && wrangler pages dev dist",
    "clean": "rm -rf dist src/generated",
    "deploy": "wrangler pages deploy dist",
    "test": "bun test tests"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "shiki": "^4.4.3",
    "typescript": "~7.0.2",
    "vite": "^8.2.2",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "mother-mask": "file:../packages/mother-mask",
    "nuclo": "0.2.30"
  }
}
