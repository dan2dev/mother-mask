# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# Build output: the client bundle + manifest that src/app-handler.ts renders
# against. No separate SSR build — Wrangler bundles src/app-handler.ts itself
# when deploying functions/[[path]].ts.
dist

# Wrangler's local cache and dry-run state.
.wrangler

*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
